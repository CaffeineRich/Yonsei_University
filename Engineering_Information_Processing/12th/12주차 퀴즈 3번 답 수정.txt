#include<stdio.h>
#include<string.h>
int main()
{
	char str[100];
	char change[100];
	char temp;
	int i,j,n;
	printf("문자열을 입력하세요\n");
	scanf("%s",str);
	n=strlen(str);
	for(i=0;i<n;i++){
		change[i]=str[n-1-i];
	}
	change[n]='\0';
	printf("변경된 문자열은 %s 입니다.\n",change);
	return 0;
}